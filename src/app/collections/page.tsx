import * as React from 'react';

export interface CollectionListProps {
}

export default function Collections (props: CollectionListProps) {
  return (
    <div>
      List collection
    </div>
  );
}
